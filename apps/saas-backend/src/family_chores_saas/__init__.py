"""SaaS deployment scaffold for Family Chores — Phase 3 placeholder.

Step 10 of the Phase 2 refactor will add `app_factory.py` that calls
`create_app(auth_strategy=PlaceholderAuthStrategy(), bridge=NoOpBridge(), ...)`
and exposes a single `/health` endpoint.

See `DECISIONS.md` §11.
"""

__version__ = "0.1.0"
