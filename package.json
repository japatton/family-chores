{
  "name": "family-chores-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Family Chores monorepo root — see DECISIONS §11.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
