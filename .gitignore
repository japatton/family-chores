__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.env
.env.*
!.env.example

.mypy_cache/
.pytest_cache/
.ruff_cache/
*.tsbuildinfo

dist/
build/
family_chores/frontend/dist/
lovelace-card/dist/

# Built SPA output — produced by `npm run build` locally or the
# frontend-build Docker stage. The directory doesn't exist in a fresh
# clone; FastAPI's static mount checks for `index.html` specifically
# and falls through to a helpful "run `npm run build`" message.
family_chores/src/family_chores_addon/static/

node_modules/

.DS_Store
*.log

# Local dev data dir
/data/
/tmp/
local-data/
*.tsbuildinfo
family_chores/frontend/vite.config.js
reviews/
